{% block stylesheets %}
    <style>
        .map-picker-header { background: var(--light-pink); padding: 0.5rem 1rem; border-radius: 8px 8px 0 0; }
        .btn-light-pink { background: var(--light-pink); color: var(--primary-pink); border: 1px solid var(--medium-pink); }
        .btn-light-pink:hover { background: var(--medium-pink); color: var(--dark-pink); }
        #location-map { transition: opacity 0.3s ease; height: 350px; width: 100%; border-radius: 0 0 12px 12px; border: 2px solid var(--light-pink); z-index: 10; }
        .invalid-feedback, .alert-danger, .text-danger, ul li { color: #dc3545 !important; }
        .form-group ul { list-style: none; padding-left: 0; margin-top: 5px; }
    </style>
{% endblock %}

{{ form_start(form, {'attr': {'class': 'event-form', 'id': 'event-form', 'novalidate': 'novalidate'}}) }}

{% if not form.vars.valid %}
    <div class="alert alert-danger mb-4 shadow-sm border-0" style="border-radius: 12px;">
        <div class="d-flex align-items-center">
            <i class="fas fa-exclamation-circle fa-2x me-3"></i>
            <div>
                <h5 class="alert-heading mb-1">Attention</h5>
                <p class="mb-0">Veuillez corriger les erreurs dans le formulaire pour continuer.</p>
                {{ form_errors(form) }}
            </div>
        </div>
    </div>
{% endif %}

<div class="form-sections">
    <!-- Section 1: Informations de base -->
    <div class="form-section">
        <div class="section-header">
            <div class="section-icon">
                <i class="fas fa-info-circle"></i>
            </div>
            <h3>Informations de base</h3>
        </div>
        
        <div class="section-content">
            <div class="row">
                <div class="col-md-8">
                    <div class="form-group">
                        {{ form_label(form.title, 'Titre de l\'événement', {'label_attr': {'class': 'form-label'}}) }}
                        {{ form_widget(form.title, {'attr': {
                            'class': 'form-control ' ~ (form.title.vars.valid ? '' : 'is-invalid'),
                            'placeholder': 'Entrez le titre de votre événement'
                        }}) }}
                        <div class="form-hint">
                            <i class="fas fa-lightbulb"></i>
                            <span>Un titre clair et attractif qui donne envie</span>
                        </div>
                        {{ form_errors(form.title) }}
                    </div>
                </div>
            </div>
            
            <div class="form-group">
                {{ form_label(form.description, 'Description', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.description, {'attr': {
                    'class': 'form-control ' ~ (form.description.vars.valid ? '' : 'is-invalid'),
                    'rows': 5,
                    'placeholder': 'Décrivez votre événement en détail...'
                }}) }}
                <div class="form-hint with-counter">
                    <i class="fas fa-align-left"></i>
                    <span>Détails, programme, intervenants, informations importantes</span>
                    <span class="char-counter">0/1000</span>
                </div>
                {{ form_errors(form.description) }}
            </div>
        </div>
    </div>

    <!-- Section 2: Photos de l'événement -->
    <div class="form-section">
        <div class="section-header">
            <div class="section-icon">
                <i class="fas fa-images"></i>
            </div>
            <h3>Photos de l'événement</h3>
        </div>
        
        <div class="section-content">
            <!-- Hidden form field for selected image -->
            {{ form_widget(form.selectedImage, {'attr': {'class': 'd-none', 'id': 'event_selectedImage'}}) }}
            {{ form_errors(form.selectedImage) }}
            
            <!-- Image Gallery -->
            {% if existingImages is defined and existingImages|length > 0 %}
            <div class="form-group">
                <label class="form-label">Sélectionnez une image</label>
                
                <div class="image-gallery-container">
                    <!-- Gallery View -->
                    <div class="image-gallery-view active" id="galleryView">
                        <div class="gallery-grid">
                            {% for image in existingImages %}
                                <div class="gallery-item" data-image="{{ image }}">
                                    <div class="gallery-card {% if form.selectedImage.vars.value is defined and form.selectedImage.vars.value == image %}selected{% endif %}">
                                        <img src="{{ asset('img/events/' ~ image) }}" 
                                             alt="{{ image }}"
                                             class="gallery-image"
                                             onerror="this.src='data:image/svg+xml;utf8,<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"100\" height=\"100\" viewBox=\"0 0 100 100\"><rect width=\"100\" height=\"100\" fill=\"%23f0f0f0\"/><text x=\"50\" y=\"50\" text-anchor=\"middle\" dy=\".3em\" font-family=\"Arial\" font-size=\"10\" fill=\"%23999\">Image</text></svg>';">
                                        <div class="gallery-overlay">
                                            <button type="button" class="btn-select-image">
                                                <i class="fas fa-check"></i>
                                            </button>
                                        </div>
                                        <div class="gallery-check">
                                            <i class="fas fa-check-circle"></i>
                                        </div>
                                    </div>
                                </div>
                            {% endfor %}
                        </div>
                    </div>
                    
                    <div class="form-hint">
                        <i class="fas fa-info-circle"></i>
                        <span>Sélectionnez une image pour votre événement</span>
                    </div>
                </div>
            </div>
            {% else %}
            <div class="no-images-message">
                <i class="fas fa-exclamation-triangle"></i>
                <p>Aucune image disponible dans le dossier. Veuillez ajouter des images dans le dossier /public/img/.</p>
            </div>
            {% endif %}
        </div>
    </div>

    <!-- Section 3: Date et heure -->
    <div class="form-section">
        <div class="section-header">
            <div class="section-icon">
                <i class="fas fa-calendar-alt"></i>
            </div>
            <h3>Date et heure</h3>
        </div>
        
        <div class="section-content">
            <!-- Mode Toggle -->
            <div class="form-group mb-4">
                <div class="mode-selector">
                    <div class="form-check form-switch custom-switch-large">
                        {{ form_widget(form.isWeekly, {'attr': {'class': 'form-check-input', 'id': 'is-weekly-toggle'}}) }}
                        {{ form_label(form.isWeekly, 'Événement hebdomadaire', {'label_attr': {'class': 'form-check-label fw-bold'}}) }}
                    </div>
                </div>
                <div class="form-hint">
                    <i class="fas fa-info-circle"></i>
                    <span>Choisissez si cet événement est une séance unique ou récurrente chaque semaine</span>
                </div>
            </div>

            <!-- One-time Event Section -->
            <div id="date-selection-section">
                <div class="row">
                    <div class="col-md-6">
                        <div class="form-group">
                            <label for="{{ form.startAt.vars.id }}" class="form-label">
                                <i class="fas fa-play-circle"></i>
                                Date et heure de début
                            </label>
                            <div class="datetime-card">
                                <div class="datetime-input-wrapper">
                                    {{ form_widget(form.startAt, {'attr': {
                                        'class': 'form-control datetime-input ' ~ (form.startAt.vars.valid ? '' : 'is-invalid'),
                                        'placeholder': 'YYYY-MM-DD HH:MM'
                                    }}) }}
                                    <button type="button" class="datetime-trigger" data-target="{{ form.startAt.vars.id }}">
                                        <i class="fas fa-calendar-alt"></i>
                                    </button>
                                </div>
                                <div class="datetime-preview" id="startAt-preview">
                                    <i class="fas fa-clock"></i>
                                    <span>Sélectionnez une date</span>
                                </div>
                            </div>
                            {{ form_errors(form.startAt) }}
                        </div>
                    </div>
                    
                    <div class="col-md-6">
                        <div class="form-group">
                            <label for="{{ form.endAt.vars.id }}" class="form-label">
                                <i class="fas fa-stop-circle"></i>
                                Date et heure de fin
                            </label>
                            <div class="datetime-card">
                                <div class="datetime-input-wrapper">
                                    {{ form_widget(form.endAt, {'attr': {
                                        'class': 'form-control datetime-input ' ~ (form.endAt.vars.valid ? '' : 'is-invalid'),
                                        'placeholder': 'YYYY-MM-DD HH:MM'
                                    }}) }}
                                    <button type="button" class="datetime-trigger" data-target="{{ form.endAt.vars.id }}">
                                        <i class="fas fa-calendar-alt"></i>
                                    </button>
                                </div>
                                <div class="datetime-preview" id="endAt-preview">
                                    <i class="fas fa-clock"></i>
                                    <span>Sélectionnez une date</span>
                                </div>
                            </div>
                            {{ form_errors(form.endAt) }}
                        </div>
                    </div>
                </div>

            </div>

            <!-- Weekly Event Section -->
            <div id="weekly-selection-section" style="display: none;">
                <div class="row">
                    <div class="col-md-4">
                        <div class="form-group">
                            {{ form_label(form.dayOfWeek, 'Jour de la semaine', {'label_attr': {'class': 'form-label'}}) }}
                            <div class="select-wrapper">
                                {{ form_widget(form.dayOfWeek, {'attr': {'class': 'form-select'}}) }}
                                <i class="fas fa-chevron-down select-arrow"></i>
                            </div>
                            {{ form_errors(form.dayOfWeek) }}
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="form-group">
                            {{ form_label(form.startTime, 'Heure de début', {'label_attr': {'class': 'form-label'}}) }}
                            <div class="input-with-icon">
                                {{ form_widget(form.startTime, {'attr': {'class': 'form-control'}}) }}
                                <i class="fas fa-clock input-icon"></i>
                            </div>
                            {{ form_errors(form.startTime) }}
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="form-group">
                            {{ form_label(form.endTime, 'Heure de fin', {'label_attr': {'class': 'form-label'}}) }}
                            <div class="input-with-icon">
                                {{ form_widget(form.endTime, {'attr': {'class': 'form-control'}}) }}
                                <i class="fas fa-clock input-icon"></i>
                            </div>
                            {{ form_errors(form.endTime) }}
                        </div>
                    </div>
                </div>
            </div>

            <div class="duration-display" id="duration-display">
                <div class="duration-card">
                    <div class="duration-icon">
                        <i class="fas fa-hourglass-half"></i>
                    </div>
                    <div class="duration-content">
                        <span class="duration-label">Durée estimée :</span>
                        <span class="duration-value" id="duration-value">--</span>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Section 3.5: Matériel Nécessaire -->
    <div class="form-section">
        <div class="section-header">
            <div class="section-icon">
                <i class="fas fa-box-open"></i>
            </div>
            <h3>Matériel Nécessaire</h3>
        </div>
        
        <div class="section-content">
            <div class="form-group mb-4">
                <div class="mode-selector">
                    <div class="form-check form-switch custom-switch-large reversed-layout">
                        <span class="custom-switch-text fw-bold">Besoin de matériel spécifique ?</span>
                        {{ form_widget(form.showRequirements, {'label': false, 'attr': {'class': 'form-check-input requirements-toggle', 'id': 'requirement-toggle'}}) }}
                    </div>
                </div>
                <div class="form-hint">
                    <i class="fas fa-info-circle"></i>
                    <span>Cochez si vous avez besoin que les participants apportent du matériel</span>
                </div>
            </div>

            <div id="requirements-selection-section" style="display: none;">
                <div class="requirements-selection-grid mt-4">
                    <div class="requirements-grid">
                        {% for requirement in form.requirements %}
                            <div class="requirement-card" data-id="{{ requirement.vars.id }}">
                                <div class="requirement-card-body">
                                    <div class="requirement-icon-wrapper">
                                        <div class="requirement-icon">
                                            <i class="fas fa-magic"></i>
                                        </div>
                                    </div>
                                    <div class="requirement-info">
                                        <span class="requirement-name">{{ requirement.vars.label }}</span>
                                        <span class="requirement-details">Matériel à prévoir</span>
                                    </div>
                                    <div class="requirement-action">
                                        {{ form_widget(requirement, {'label': false, 'attr': {'class': 'd-none requirement-checkbox'}}) }}
                                        <div class="custom-checkbox-indicator">
                                            <i class="fas fa-plus"></i>
                                        </div>
                                    </div>
                                </div>
                                <div class="requirement-selected-badge">
                                    <i class="fas fa-check"></i>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                    {{ form_errors(form.requirements) }}
                </div>
            </div>
        </div>
    </div>

    <!-- Section 4: Localisation -->
    <div class="form-section">
        <div class="section-header">
            <div class="section-icon">
                <i class="fas fa-map-marker-alt"></i>
            </div>
            <h3>Localisation</h3>
        </div>
        
        <div class="section-content">
            <div class="form-group">
                {{ form_label(form.location, 'Lieu', {'label_attr': {'class': 'form-label'}}) }}
                <div class="input-with-button">
                    {{ form_widget(form.location, {'attr': {'class': 'form-control', 'placeholder': 'ex: Salle A, Rez-de-chaussée', 'id': 'event_location_input'}}) }}
                    <button class="btn btn-integrated" type="button" id="toggle-map-picker" 
                            title="Choisir sur la carte">
                        <i class="fas fa-map-marked-alt"></i> Carte
                    </button>
                </div>
                
                <div id="map-picker-container" style="display: none; margin-top: 1rem;">
                    <div class="map-picker-header mb-2 d-flex justify-content-between align-items-center">
                        <span class="text-xs text-muted"><i class="fas fa-info-circle me-1"></i> Cliquez ou déplacez le marqueur pour définir l'adresse</span>
                        <button type="button" class="btn btn-sm btn-light-pink rounded-pill" id="close-map-picker">Fermer</button>
                    </div>
                    <div id="location-map" style="height: 300px; width: 100%; border-radius: 12px; border: 2px solid var(--light-pink); z-index: 10;"></div>
                </div>
                <div class="form-hint">
                    <i class="fas fa-info-circle"></i>
                    <span>Entrez une adresse physique ou un lien pour un événement en ligne</span>
                </div>
                {{ form_errors(form.location) }}
            </div>

            <!-- Indoor/Outdoor Toggle & Weather -->
            <div class="form-group mt-4" id="outdoor-section">
                <div class="mode-selector">
                    <div class="form-check form-switch custom-switch-large reversed-layout">
                        <span class="custom-switch-text fw-bold">Événement en extérieur ?</span>
                        {{ form_widget(form.isOutdoor, {'label': false, 'attr': {'class': 'form-check-input', 'id': 'outdoor-toggle'}}) }}
                    </div>
                </div>
                
                <div id="weather-widget-container" style="display: none; margin-top: 1rem;">
                    <div class="weather-widget premium-weather-card">
                        <div class="weather-card-header">
                            <span class="weather-title"><i class="fas fa-cloud-sun me-2"></i>Météo prévue</span>
                            <span id="weather-date" class="weather-date-badge"></span>
                        </div>
                        <div class="weather-card-body">
                            <div id="weather-loading" class="text-center py-3">
                                <div class="spinner-border text-pink spinner-border-sm" role="status"></div>
                                <span class="ms-2">Chargement de la météo...</span>
                            </div>
                            <div id="weather-data" style="display: none;">
                                <div class="d-flex align-items-center justify-content-between">
                                    <div class="weather-main">
                                        <img id="weather-icon" src="" alt="Icon" />
                                        <div class="weather-temp-group">
                                            <span id="weather-temp" class="weather-temp">--°C</span>
                                            <span id="weather-desc" class="weather-desc">--</span>
                                        </div>
                                    </div>
                                    <div class="weather-details">
                                        <div class="weather-detail-item">
                                            <i class="fas fa-tint"></i>
                                            <span id="weather-humidity">--%</span>
                                        </div>
                                        <div class="weather-detail-item">
                                            <i class="fas fa-wind"></i>
                                            <span id="weather-wind">-- km/h</span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div id="weather-error" class="alert alert-soft-danger py-2 mb-0 mt-2" style="display: none; font-size: 0.85rem;">
                                <i class="fas fa-exclamation-circle me-1"></i>
                                <span id="weather-error-msg">Impossible de récupérer la météo.</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Section 5: Catégorie de Eventment -->
    <div class="form-section">
        <div class="section-header">
            <div class="section-icon">
                <i class="fas fa-list"></i>
            </div>
            <h3>Catégorie de Eventment</h3>
        </div>
        
        <div class="section-content">
            <div class="form-group">
                {{ form_label(form.eventCat, 'Sélectionnez une catégorie', {'label_attr': {'class': 'form-label'}}) }}
                <div class="select-wrapper">
                    {{ form_widget(form.eventCat, {'attr': {
                        'class': 'form-select ' ~ (form.eventCat.vars.valid ? '' : 'is-invalid')
                    }}) }}
                    <i class="fas fa-chevron-down select-arrow"></i>
                </div>
                <div class="form-hint">
                    <i class="fas fa-tags"></i>
                    <span>Choisissez la catégorie qui correspond le mieux à votre événement</span>
                </div>
                {{ form_errors(form.eventCat) }}
            </div>
            
            <div class="form-group mt-3">
                {{ form_label(form.capacity, 'Nombre maximum de participants', {'label_attr': {'class': 'form-label'}}) }}
                <div class="input-with-icon">
                    {{ form_widget(form.capacity, {'attr': {
                        'class': 'form-control ' ~ (form.capacity.vars.valid ? '' : 'is-invalid'),
                        'placeholder': 'ex: 20'
                    }}) }}
                    <i class="fas fa-users input-icon"></i>
                </div>
                <div class="form-hint">
                    <i class="fas fa-info-circle"></i>
                    <span>Laissez vide pour un nombre de places illimité</span>
                </div>
                {{ form_errors(form.capacity) }}
            </div>
        </div>
    </div>
</div>

{{ form_end(form) }}

<style>
/* Pink theme variables - Keeping original color scheme */
:root {
    --primary-pink: #ec4899;
    --light-pink: #fce7f3;
    --medium-pink: #fbcfe8;
    --dark-pink: #be185d;
    --error-red: #ef4444;
    --error-light: #fee2e2;
    --success-green: #10b981;
    --text-color: #374151;
    --border-color: #e5e7eb;
    --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
    --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
    --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
}

/* Image Gallery Styles */
.image-gallery-container {
    margin-top: 1rem;
}

.image-gallery-view.active {
    display: block;
    animation: fadeIn 0.3s ease;
}

@keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
}

/* Gallery Grid */
.gallery-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
    gap: 1rem;
    margin-top: 1rem;
}

.gallery-item {
    position: relative;
}

.gallery-card {
    background: white;
    border: 2px solid var(--light-pink);
    border-radius: 8px;
    overflow: hidden;
    transition: all 0.3s ease;
    position: relative;
    cursor: pointer;
    aspect-ratio: 1;
}

.gallery-card:hover {
    border-color: var(--primary-pink);
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
}

.gallery-card.selected {
    border-color: var(--primary-pink);
    background: #fdf2f8;
}

.gallery-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.gallery-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(236, 72, 153, 0.8);
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    transition: opacity 0.3s ease;
}

.gallery-card:hover .gallery-overlay {
    opacity: 1;
}

.btn-select-image {
    background: white;
    border: none;
    width: 36px;
    height: 36px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--primary-pink);
    cursor: pointer;
    transition: all 0.3s ease;
}

.btn-select-image:hover {
    background: var(--dark-pink);
    color: white;
    transform: scale(1.1);
}

.gallery-check {
    position: absolute;
    top: 0.5rem;
    right: 0.5rem;
    background: var(--success-green);
    color: white;
    width: 24px;
    height: 24px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    transform: scale(0);
    transition: all 0.3s ease;
}

.gallery-card.selected .gallery-check {
    opacity: 1;
    transform: scale(1);
}

.image-filename {
    font-size: 0.75rem;
    color: var(--text-color);
    margin-top: 0.5rem;
    text-align: center;
    word-break: break-all;
    max-height: 2.5em;
    overflow: hidden;
    text-overflow: ellipsis;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
}

.no-images-message {
    text-align: center;
    padding: 2rem;
    background: var(--light-pink);
    border-radius: 8px;
    border: 2px dashed var(--medium-pink);
}

.no-images-message i {
    font-size: 2.5rem;
    color: #f59e0b;
    margin-bottom: 1rem;
}

.no-images-message p {
    color: var(--text-color);
    margin: 0;
    font-weight: 500;
}

.d-none {
    display: none !important;
}

/* Mode Selector Styling */
.mode-selector {
    background: var(--light-pink);
    padding: 1.25rem;
    border-radius: 12px;
    border: 2px solid var(--medium-pink);
    margin-bottom: 0.5rem;
}

.custom-switch-large.reversed-layout {
    display: flex;
    align-items: center;
    flex-direction: row;
    justify-content: flex-start;
    padding-left: 0 !important;
    gap: 1rem;
}

.custom-switch-large.reversed-layout .custom-switch-text {
    font-size: 1.1rem;
    cursor: pointer;
    color: var(--dark-pink);
    order: 1;
}

.custom-switch-large.reversed-layout .form-check-input {
    position: relative;
    order: 2;
    margin-left: 0 !important;
    width: 3.5rem !important;
    height: 1.75rem !important;
    cursor: pointer;
}

.form-check-input:checked {
    background-color: var(--primary-pink);
    border-color: var(--primary-pink);
}

/* Form Section Styling */
.form-section {
    background: white;
    border: 2px solid var(--light-pink);
    border-radius: 12px;
    padding: 2rem;
    margin-bottom: 1.5rem;
    box-shadow: var(--shadow-sm);
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
}

.form-section:hover {
    box-shadow: var(--shadow-md);
    border-color: var(--medium-pink);
}

.form-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 4px;
    height: 100%;
    background: linear-gradient(135deg, var(--primary-pink), var(--dark-pink));
}

/* Section Header */
.section-header {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1.5rem;
    padding-bottom: 0.75rem;
    border-bottom: 2px solid var(--light-pink);
}

.section-icon {
    background: linear-gradient(135deg, var(--primary-pink), var(--dark-pink));
    color: white;
    width: 48px;
    height: 48px;
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.25rem;
    box-shadow: 0 4px 8px rgba(236, 72, 153, 0.2);
}

.section-header h3 {
    color: var(--dark-pink);
    font-size: 1.5rem;
    font-weight: 600;
    margin: 0;
}

/* Form Groups */
.form-group {
    margin-bottom: 1.5rem;
    position: relative;
}

.form-group:last-child {
    margin-bottom: 0;
}

/* Form Labels */
.form-label {
    color: var(--text-color);
    font-weight: 600;
    margin-bottom: 0.5rem;
    display: block;
    font-size: 1rem;
}

.form-label i {
    color: var(--primary-pink);
    margin-right: 0.5rem;
}

/* Form Controls */
.form-control, .form-select {
    border: 2px solid var(--medium-pink);
    border-radius: 8px;
    padding: 0.75rem 1rem;
    font-size: 1rem;
    transition: all 0.3s ease;
    background: white;
    color: var(--text-color);
}

.form-control:focus, .form-select:focus {
    border-color: var(--primary-pink);
    box-shadow: 0 0 0 4px rgba(236, 72, 153, 0.15);
    outline: none;
}

/* Custom Select */
.select-wrapper {
    position: relative;
}

.select-arrow {
    position: absolute;
    right: 1rem;
    top: 50%;
    transform: translateY(-50%);
    color: var(--primary-pink);
    pointer-events: none;
    transition: transform 0.3s ease;
}

.form-select:focus + .select-arrow {
    transform: translateY(-50%) rotate(180deg);
}

/* DateTime Picker */
.datetime-card {
    background: var(--light-pink);
    border-radius: 10px;
    padding: 1.25rem;
    border: 2px solid transparent;
    transition: all 0.3s ease;
}

.datetime-card:hover {
    border-color: var(--medium-pink);
}

.datetime-input-wrapper {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 1rem;
}

.datetime-input {
    flex: 1;
    background: white !important;
    border: 2px solid var(--medium-pink) !important;
    padding-right: 3.5rem;
}

.datetime-trigger {
    background: var(--primary-pink);
    color: white;
    border: none;
    border-radius: 8px;
    width: 42px;
    height: 42px;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all 0.3s ease;
    flex-shrink: 0;
}

.datetime-trigger:hover {
    background: var(--dark-pink);
    transform: translateY(-2px);
    box-shadow: 0 4px 8px rgba(236, 72, 153, 0.3);
}

.datetime-preview {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.75rem 1rem;
    background: white;
    border-radius: 8px;
    border: 2px solid var(--light-pink);
    font-size: 0.95rem;
}

.datetime-preview i {
    color: var(--primary-pink);
    font-size: 1rem;
}

/* Duration Display */
.duration-display {
    margin-top: 1.5rem;
}

.duration-card {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.75rem 1.25rem;
    background: var(--light-pink);
    border-radius: 10px;
    border: 2px solid var(--medium-pink);
}

.duration-icon {
    background: linear-gradient(135deg, var(--primary-pink), var(--dark-pink));
    color: white;
    width: 36px;
    height: 36px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
}

.duration-label {
    color: var(--text-color);
    font-weight: 500;
    font-size: 0.95rem;
}

.duration-value {
    color: var(--dark-pink);
    font-weight: 700;
    font-size: 1.1rem;
}

/* Input with Icon */
.input-with-icon {
    position: relative;
}

.input-icon {
    position: absolute;
    left: 1rem;
    top: 50%;
    transform: translateY(-50%);
    color: var(--primary-pink);
}

.input-with-icon .form-control {
    padding-left: 2.75rem;
}

/* Input with Integrated Button */
.input-with-button {
    position: relative;
    display: flex;
    align-items: center;
}

.input-with-button .form-control {
    padding-right: 6rem;
}

.input-with-button .btn-integrated {
    position: absolute;
    right: 8px;
    top: 50%;
    transform: translateY(-50%);
    border: none;
    background: var(--light-pink);
    color: var(--primary-pink);
    padding: 0.4rem 0.9rem;
    font-size: 0.85rem;
    font-weight: 700;
    border-radius: 8px;
    z-index: 4;
    transition: all 0.2s ease;
    display: flex;
    align-items: center;
    gap: 0.4rem;
}

.input-with-button .btn-integrated:hover {
    background: var(--medium-pink);
    color: var(--dark-pink);
    transform: translateY(-50%) scale(1.02);
}

.input-with-button .btn-integrated i {
    font-size: 0.9rem;
}

/* Form Hints */
.form-hint {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: #6b7280;
    font-size: 0.875rem;
    margin-top: 0.5rem;
    padding: 0.5rem 0;
}

.form-hint i {
    color: var(--primary-pink);
    font-size: 0.875rem;
}

.form-hint.with-counter {
    justify-content: space-between;
}

.char-counter {
    color: var(--text-color);
    font-weight: 500;
    font-size: 0.875rem;
}

/* Error States */
.form-control.is-invalid,
.form-select.is-invalid {
    border-color: var(--error-red);
    background-image: none !important;
    padding-right: 1rem;
}

.form-control.is-invalid:focus,
.form-select.is-invalid:focus {
    border-color: var(--error-red);
    box-shadow: 0 0 0 4px rgba(239, 68, 68, 0.15);
}

.invalid-feedback {
    display: block;
    width: 100%;
    margin-top: 0.5rem;
    font-size: 0.875rem;
    color: var(--error-red);
    background: var(--error-light);
    padding: 0.75rem 1rem;
    border-radius: 8px;
    border-left: 3px solid var(--error-red);
    animation: slideIn 0.3s ease;
    font-weight: 500;
}

.invalid-feedback ul {
    list-style: none;
    margin: 0;
    padding: 0;
}

.invalid-feedback li {
    display: flex;
    align-items: center;
    gap: 0.5rem;
}

.invalid-feedback li::before {
    content: "⚠️";
    font-size: 0.875rem;
}

/* Animations */
@keyframes slideIn {
    from {
        opacity: 0;
        transform: translateX(-10px);
    }
    to {
        opacity: 1;
        transform: translateX(0);
    }
}

/* Responsive Design */
@media (max-width: 768px) {
    .event-form {
        padding: 0;
    }
    
    .form-section {
        padding: 1.5rem;
        border-radius: 10px;
    }
    
    .datetime-input-wrapper {
        flex-direction: column;
        gap: 0.75rem;
    }
    
    .datetime-trigger {
        width: 100%;
        height: 42px;
    }
    
    .datetime-input {
        padding-right: 1rem;
    }
    
    .section-header {
        flex-wrap: wrap;
    }
    
    .section-icon {
        width: 42px;
        height: 42px;
        font-size: 1.1rem;
    }
    
    .gallery-grid {
        grid-template-columns: repeat(auto-fill, minmax(100px, 1fr));
        gap: 0.75rem;
    }
}

@media (max-width: 480px) {
    .gallery-grid {
        grid-template-columns: repeat(auto-fill, minmax(80px, 1fr));
        gap: 0.5rem;
    }
}

/* Row spacing */
.row {
    margin-left: -0.5rem;
    margin-right: -0.5rem;
}

.row > [class*="col-"] {
    padding-left: 0.5rem;
    padding-right: 0.5rem;
}

/* Premium Weather Card Styling */
.premium-weather-card {
    background: white;
    border-radius: 16px;
    border: 2px solid var(--light-pink);
    overflow: hidden;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
    transition: all 0.3s ease;
    margin-top: 1rem;
}

.premium-weather-card:hover {
    box-shadow: 0 10px 15px -3px rgba(236, 72, 153, 0.1);
    border-color: var(--medium-pink);
}

.weather-card-header {
    background: var(--light-pink);
    padding: 0.75rem 1.25rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 2px solid var(--medium-pink);
}

.weather-title {
    font-weight: 700;
    color: var(--dark-pink);
    font-size: 0.95rem;
}

.weather-date-badge {
    background: white;
    color: var(--primary-pink);
    padding: 0.25rem 0.75rem;
    border-radius: 20px;
    font-size: 0.8rem;
    font-weight: 600;
}

.weather-card-body {
    padding: 1.25rem;
}

.weather-main {
    display: flex;
    align-items: center;
    gap: 1rem;
}

.weather-temp-group {
    display: flex;
    flex-direction: column;
}

.weather-temp {
    font-size: 1.75rem;
    font-weight: 800;
    color: var(--text-dark);
}

.weather-desc {
    font-size: 0.9rem;
    color: var(--text-muted);
    text-transform: capitalize;
}

.weather-details {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
}

.weather-detail-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--text-muted);
    font-size: 0.85rem;
}

.weather-detail-item i {
    color: var(--primary-pink);
    width: 16px;
}


/* Make sure form controls are properly sized on mobile */
@media (max-width: 576px) {
    .form-control, .form-select {
        font-size: 16px; /* Prevents zoom on iOS */
    }
}

/* Requirements Selection Grid */
.requirements-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
    gap: 1.5rem;
}

.requirement-card {
    position: relative;
    background: rgba(255, 255, 255, 0.8);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    border: 1px solid rgba(236, 72, 153, 0.1);
    border-radius: 20px;
    padding: 1.75rem 1.5rem;
    cursor: pointer;
    transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.03);
}

.requirement-card:hover {
    border-color: var(--primary-pink);
    transform: translateY(-6px) scale(1.02);
    box-shadow: 0 15px 30px -10px rgba(236, 72, 153, 0.2);
    background: white;
}

.requirement-card.selected {
    border-color: var(--primary-pink);
    background: linear-gradient(135deg, #fff 0%, var(--light-pink) 100%);
    box-shadow: 0 12px 25px -5px rgba(236, 72, 153, 0.15), 0 0 0 1px rgba(236, 72, 153, 0.1);
}

.requirement-card-body {
    display: flex;
    align-items: center;
    gap: 1.1rem;
}

.requirement-icon-wrapper {
    position: relative;
    z-index: 1;
}

.requirement-icon {
    width: 36px;
    height: 36px;
    background: linear-gradient(135deg, white 0%, var(--light-pink) 100%);
    color: var(--primary-pink);
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1rem;
    transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
    box-shadow: 0 4px 8px -2px rgba(236, 72, 153, 0.1);
    border: 1px solid rgba(236, 72, 153, 0.1);
}

.requirement-card:hover .requirement-icon {
    background: linear-gradient(135deg, var(--primary-pink), var(--dark-pink));
    color: white;
    transform: rotate(8deg) scale(1.05);
    box-shadow: 0 8px 15px -4px rgba(236, 72, 153, 0.3);
    border-color: transparent;
}

.requirement-card.selected .requirement-icon {
    background: var(--primary-pink);
    color: white;
}

.requirement-info {
    flex: 1;
    display: flex;
    flex-direction: column;
}

.requirement-name {
    font-weight: 700;
    color: var(--text-dark);
    font-size: 1rem;
    transition: all 0.3s ease;
    line-height: 1.2;
}

.requirement-card.selected .requirement-name {
    color: var(--dark-pink);
    transform: translateX(2px);
}

.requirement-details {
    font-size: 0.78rem;
    color: var(--text-muted);
    margin-top: 4px;
    font-weight: 500;
}

.requirement-action {
    display: flex;
    align-items: center;
}

.custom-checkbox-indicator {
    width: 28px;
    height: 28px;
    border: 2px solid #f1f5f9;
    background: #f8fafc;
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.850rem;
    color: #cbd5e1;
    transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
}

.requirement-card:hover .custom-checkbox-indicator {
    border-color: var(--medium-pink);
    color: var(--primary-pink);
    transform: scale(1.1);
}

.requirement-card.selected .custom-checkbox-indicator {
    background: var(--primary-pink);
    border-color: var(--primary-pink);
    color: white;
    transform: scale(1) rotate(90deg);
}

.requirement-selected-badge {
    position: absolute;
    top: -12px;
    right: -12px;
    width: 32px;
    height: 32px;
    background: linear-gradient(135deg, var(--primary-pink), var(--dark-pink));
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.9rem;
    box-shadow: 0 6px 15px rgba(236, 72, 153, 0.4);
    opacity: 0;
    transform: scale(0.5) rotate(-90deg);
    transition: all 0.5s cubic-bezier(0.34, 1.56, 0.64, 1);
    z-index: 10;
    border: 3px solid white;
}

.requirement-card.selected .requirement-selected-badge {
    opacity: 1;
    transform: scale(1) rotate(0deg);
}

@keyframes slideInUp {
    from {
        opacity: 0;
        transform: translateY(20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}
</style>

<!-- Include Flatpickr -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
<script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
<script src="https://cdn.jsdelivr.net/npm/flatpickr/dist/l10n/fr.js"></script>

<script>
document.addEventListener('DOMContentLoaded', function() {
    console.log('Maternia Form: Initializing all scripts...');

    // ===== 1. Weekly Toggle Logic =====
    const isWeeklyToggle = document.getElementById('is-weekly-toggle') || document.querySelector('[name*="[isWeekly]"]');
    const dateSection = document.getElementById('date-selection-section');
    const weeklySection = document.getElementById('weekly-selection-section');
    const startAtInput = document.querySelector('[name*="[startAt]"]');
    const endAtInput = document.querySelector('[name*="[endAt]"]');
    const dayOfWeekSelect = document.querySelector('[name*="[dayOfWeek]"]');
    const startTimeInput = document.querySelector('[name*="[startTime]"]');
    const endTimeInput = document.querySelector('[name*="[endTime]"]');

    function toggleSections() {
        if (!isWeeklyToggle) return;
        const isChecked = isWeeklyToggle.checked;
        const outdoorSection = document.getElementById('outdoor-section');
        if (isChecked) {
            if (dateSection) dateSection.style.display = 'none';
            if (weeklySection) weeklySection.style.display = 'block';
            if (startAtInput) { startAtInput.required = false; startAtInput.value = ''; }
            if (endAtInput) { endAtInput.required = false; endAtInput.value = ''; }
            if (dayOfWeekSelect) dayOfWeekSelect.required = true;
            if (startTimeInput) startTimeInput.required = true;
            if (endTimeInput) endTimeInput.required = true;
            // Hide outdoor/weather for weekly events (no specific date)
            if (outdoorSection) outdoorSection.style.display = 'none';
        } else {
            if (dateSection) dateSection.style.display = 'block';
            if (weeklySection) weeklySection.style.display = 'none';
            if (startAtInput) startAtInput.required = true;
            if (endAtInput) endAtInput.required = true;
            if (dayOfWeekSelect) { dayOfWeekSelect.required = false; dayOfWeekSelect.value = ''; }
            if (startTimeInput) { startTimeInput.required = false; startTimeInput.value = ''; }
            if (endTimeInput) { endTimeInput.required = false; endTimeInput.value = ''; }
            // Show outdoor/weather for one-time events
            if (outdoorSection) outdoorSection.style.display = 'block';
        }
        updateDuration();
    }

    if (isWeeklyToggle) {
        isWeeklyToggle.addEventListener('change', toggleSections);
        isWeeklyToggle.addEventListener('click', toggleSections);
        toggleSections(); // Initial state
    }

    // 2. Requirements Toggle Logic
    function initRequirementsToggle() {
        const reqToggle = document.getElementById('requirement-toggle') || 
                          document.querySelector('.requirements-toggle') || 
                          document.querySelector('input[name*="showRequirements"]');
        const reqSection = document.getElementById('requirements-selection-section');
        const requirementCards = document.querySelectorAll('.requirement-card');

        function updateVisibility() {
            if (!reqSection) return;
            const isChecked = reqToggle ? reqToggle.checked : false;
            console.log('Requirements Visibility: isChecked =', isChecked);
            if (isChecked) {
                reqSection.style.setProperty('display', 'block', 'important');
                reqSection.classList.add('animate__animated', 'animate__fadeIn');
            } else {
                reqSection.style.setProperty('display', 'none', 'important');
            }
        }

        if (reqToggle) {
            reqToggle.addEventListener('change', updateVisibility);
            reqToggle.addEventListener('click', updateVisibility);
            updateVisibility();
        }

        requirementCards.forEach(card => {
            const checkbox = card.querySelector('.requirement-checkbox');
            if (checkbox) {
                card.addEventListener('click', function() {
                    checkbox.checked = !checkbox.checked;
                    card.classList.toggle('selected', checkbox.checked);
                    checkbox.dispatchEvent(new Event('change', { bubbles: true }));
                });
                if (checkbox.checked) card.classList.add('selected');
            }
        });

        const anyChecked = Array.from(requirementCards).some(c => c.querySelector('.requirement-checkbox')?.checked);
        if (anyChecked && reqToggle && !reqToggle.checked) {
            reqToggle.checked = true;
            updateVisibility();
        }
    }
    initRequirementsToggle();

    function initImageGallery() {
        const galleryContainer = document.querySelector('.gallery-grid');
        const selectedImageInput = document.getElementById('event_selectedImage');
        if (!galleryContainer || !selectedImageInput) return;

        galleryContainer.addEventListener('click', function(e) {
            const galleryItem = e.target.closest('.gallery-item');
            if (!galleryItem) return;
            const imageName = galleryItem.dataset.image;
            document.querySelectorAll('.gallery-card').forEach(card => card.classList.remove('selected'));
            const card = galleryItem.querySelector('.gallery-card');
            if (card) card.classList.add('selected');
            selectedImageInput.value = imageName;
            selectedImageInput.dispatchEvent(new Event('change', { bubbles: true }));
        });

        if (selectedImageInput.value) {
            const item = galleryContainer.querySelector(`.gallery-item[data-image="${selectedImageInput.value}"]`);
            if (item) item.querySelector('.gallery-card')?.classList.add('selected');
        }
    }
    initImageGallery();

    function updateDuration() {
        const isWeekly = isWeeklyToggle && isWeeklyToggle.checked;
        const durationValue = document.getElementById('duration-value');
        if (!durationValue) return;

        const startV = (isWeekly ? (document.querySelector('[name*="startTime"]')?.value) : (document.querySelector('[name*="startAt"]')?.value)) || '';
        const endV = (isWeekly ? (document.querySelector('[name*="endTime"]')?.value) : (document.querySelector('[name*="endAt"]')?.value)) || '';

        if (!startV || !endV) { durationValue.textContent = '--'; return; }

        let startD = isWeekly ? new Date('2000-01-01T' + startV) : new Date(startV.replace(' ', 'T'));
        let endD = isWeekly ? new Date('2000-01-01T' + endV) : new Date(endV.replace(' ', 'T'));

        if (isNaN(startD.getTime()) || isNaN(endD.getTime())) { durationValue.textContent = 'Invalide'; return; }
        if (endD <= startD) { durationValue.textContent = 'Fin après début'; return; }

        const diffMs = endD - startD;
        const h = Math.floor(diffMs / 3600000);
        const m = Math.floor((diffMs % 3600000) / 60000);
        durationValue.textContent = `${h}h ${m}m`;
    }

    document.querySelectorAll('.datetime-input').forEach(input => {
        flatpickr(input, {
            enableTime: true,
            dateFormat: "Y-m-d H:i",
            time_24hr: true,
            locale: "fr",
            onChange: function() {
                updateDuration();
                if (input.name && input.name.includes('startAt')) {
                    const outdoorToggle = document.getElementById('outdoor-toggle');
                    if (outdoorToggle && outdoorToggle.checked) {
                        if (typeof fetchWeatherForLocation === 'function') fetchWeatherForLocation();
                    }
                }
            }
        });
    });

    document.querySelectorAll('.datetime-trigger').forEach(trigger => {
        trigger.addEventListener('click', function() {
            const targetId = this.dataset.target;
            const targetInput = document.getElementById(targetId);
            if (targetInput && targetInput._flatpickr) {
                targetInput._flatpickr.open();
            }
        });
    });

    document.querySelectorAll('[name*="startTime"], [name*="endTime"]').forEach(input => {
        flatpickr(input, { enableTime: true, noCalendar: true, dateFormat: "H:i", time_24hr: true, locale: "fr", onChange: updateDuration });
    });

    const desc = document.querySelector('[name*="description"]');
    if (desc) {
        const formGroup = desc.closest('.form-group');
        const counter = formGroup ? formGroup.querySelector('.char-counter') : null;
        if (counter) {
            desc.addEventListener('input', () => { counter.textContent = `${desc.value.length}/1000`; });
            counter.textContent = `${desc.value.length}/1000`;
        }
    }

    const mainForm = document.getElementById('event-form');
    if (mainForm) {
        mainForm.addEventListener('submit', function(e) {
            if (!document.getElementById('event_selectedImage')?.value) {
                e.preventDefault();
                alert('Veuillez sélectionner une image pour l\'événement.');
            }
        });
    }

    const locationInput = document.getElementById('event_location_input') || document.querySelector('[name*="[location]"]');
    const outdoorToggle = document.getElementById('outdoor-toggle') || document.querySelector('.indoor-toggle') || document.querySelector('[name*="[isOutdoor]"]');
    const weatherContainer = document.getElementById('weather-widget-container');

    let selectedLat = null;
    let selectedLng = null;

    const wmoDescriptions = {
        0: 'Ciel dégagé', 1: 'Principalement dégagé', 2: 'Partiellement nuageux', 3: 'Couvert',
        45: 'Brouillard', 48: 'Brouillard givrant',
        51: 'Bruine légère', 53: 'Bruine modérée', 55: 'Bruine forte',
        61: 'Pluie légère', 63: 'Pluie modérée', 65: 'Pluie forte',
        71: 'Neige légère', 73: 'Neige modérée', 75: 'Neige forte',
        80: 'Averses légères', 81: 'Averses modérées', 82: 'Averses violentes',
        95: 'Orage', 96: 'Orage avec grêle légère', 99: 'Orage avec grêle forte'
    };

    function wmoToIcon(code, isDay) {
        const prefix = isDay ? 'd' : 'n';
        if (code === 0) return '01' + prefix;
        if (code <= 2) return '02' + prefix;
        if (code === 3) return '04' + prefix;
        if (code <= 48) return '50' + prefix;
        if (code <= 55) return '09' + prefix;
        if (code <= 65) return '10' + prefix;
        if (code <= 75) return '13' + prefix;
        if (code <= 82) return '09' + prefix;
        return '11' + prefix;
    }

    function displayWeatherData(data, dateLabel) {
        if (!document.getElementById('weather-temp')) return;
        document.getElementById('weather-temp').textContent = Math.round(data.temp) + '°C';
        document.getElementById('weather-desc').textContent = data.desc;
        document.getElementById('weather-humidity').textContent = Math.round(data.humidity) + '%';
        document.getElementById('weather-wind').textContent = Math.round(data.wind) + ' km/h';
        document.getElementById('weather-icon').src = `https://openweathermap.org/img/wn/${data.icon}@2x.png`;
        
        if (dateLabel) {
            document.getElementById('weather-date').textContent = dateLabel;
        } else {
            document.getElementById('weather-date').textContent = new Date().toLocaleDateString('fr-FR', { weekday: 'long', day: 'numeric', month: 'long' });
        }

        document.getElementById('weather-loading').style.display = 'none';
        document.getElementById('weather-data').style.display = 'block';
    }

    // Get the event start date and time from the form for weather forecast
    function getEventDateTime() {
        const isWeeklyToggle = document.getElementById('is-weekly-toggle');
        const isWeekly = isWeeklyToggle && isWeeklyToggle.checked;
        
        if (isWeekly) {
            const daySelect = document.querySelector('select[name*="dayOfWeek"]');
            const timeInput = document.querySelector('input[name*="startTime"]');
            
            if (daySelect && daySelect.value && timeInput && timeInput.value) {
                const days = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'];
                const targetDay = days.indexOf(daySelect.value);
                
                if (targetDay !== -1) {
                    const now = new Date();
                    const resultDate = new Date();
                    // Move to the next occurrence of this day
                    let diff = (targetDay - now.getDay() + 7) % 7;
                    // If it's today but the time has passed, move to next week
                    const hour = parseInt(timeInput.value.split(':')[0], 10);
                    if (diff === 0 && hour <= now.getHours()) {
                        diff = 7;
                    }
                    resultDate.setDate(now.getDate() + diff);
                    
                    const dateStr = resultDate.toISOString().split('T')[0];
                    return { date: dateStr, hour: hour };
                }
            }
        } else {
            const startInput = document.querySelector('[name*="[startAt]"]');
            if (startInput && startInput.value) {
                const parts = startInput.value.split(' '); // "YYYY-MM-DD HH:MM"
                const dateStr = parts[0];
                const timeStr = parts[1] || null;
                if (dateStr && dateStr.match(/^\d{4}-\d{2}-\d{2}$/)) {
                    const hour = timeStr ? parseInt(timeStr.split(':')[0], 10) : null;
                    return { date: dateStr, hour: hour };
                }
            }
        }
        return null;
    }

    async function fetchWeatherByCoords(lat, lon) {
        if (!document.getElementById('weather-loading')) return;
        document.getElementById('weather-loading').style.display = 'block';
        document.getElementById('weather-data').style.display = 'none';
        const errorEl = document.getElementById('weather-error');
        if (errorEl) errorEl.style.display = 'none';

        try {
            const eventDT = getEventDateTime();
            let url;
            if (eventDT) {
                // Hourly forecast for the specific event date
                url = `https://api.open-meteo.com/v1/forecast?latitude=${lat}&longitude=${lon}&hourly=temperature_2m,relative_humidity_2m,weather_code,wind_speed_10m,is_day&start_date=${eventDT.date}&end_date=${eventDT.date}&timezone=auto`;
            } else {
                // No date set yet, show current weather
                url = `https://api.open-meteo.com/v1/forecast?latitude=${lat}&longitude=${lon}&current=temperature_2m,relative_humidity_2m,weather_code,wind_speed_10m,is_day&timezone=auto`;
            }
            const response = await fetch(url);
            const data = await response.json();

            if (data.hourly && data.hourly.time && data.hourly.time.length > 0) {
                // Find the closest hour to the event start time
                const targetHour = (eventDT && eventDT.hour !== null) ? eventDT.hour : 12;
                const h = data.hourly;
                // Hours are 0-23 in the array for that day
                const idx = Math.min(targetHour, h.time.length - 1);
                
                // Format the date + time label
                const dateObj = new Date(eventDT.date + 'T12:00:00');
                const datePart = dateObj.toLocaleDateString('fr-FR', { weekday: 'long', day: 'numeric', month: 'long', year: 'numeric' });
                const dateLabel = `${datePart} à ${targetHour}h`;

                displayWeatherData({
                    temp: h.temperature_2m[idx],
                    desc: wmoDescriptions[h.weather_code[idx]] || 'Inconnu',
                    humidity: h.relative_humidity_2m[idx],
                    wind: h.wind_speed_10m[idx],
                    icon: wmoToIcon(h.weather_code[idx], h.is_day[idx] === 1)
                }, dateLabel);
            } else if (data.current) {
                const c = data.current;
                displayWeatherData({
                    temp: c.temperature_2m,
                    desc: wmoDescriptions[c.weather_code] || 'Inconnu',
                    humidity: c.relative_humidity_2m,
                    wind: c.wind_speed_10m,
                    icon: wmoToIcon(c.weather_code, c.is_day === 1)
                });
            } else {
                throw new Error('Données météo indisponibles');
            }
        } catch (error) {
            document.getElementById('weather-loading').style.display = 'none';
            if (errorEl) {
                errorEl.style.display = 'block';
                const errorMsg = document.getElementById('weather-error-msg');
                if (errorMsg) errorMsg.textContent = error.message;
            }
        }
    }

    async function fetchWeatherForLocation() {
        // Priority 1: Use coordinates from the map marker
        if (selectedLat && selectedLng) {
            fetchWeatherByCoords(selectedLat, selectedLng);
            return;
        }

        // Priority 2: Geocode the text in the location input
        if (locationInput && locationInput.value && locationInput.value.trim()) {
            try {
                const geoUrl = `https://nominatim.openstreetmap.org/search?format=json&q=${encodeURIComponent(locationInput.value)}&limit=1`;
                const geoResponse = await fetch(geoUrl);
                const geoData = await geoResponse.json();
                if (geoData.length > 0) {
                    fetchWeatherByCoords(parseFloat(geoData[0].lat), parseFloat(geoData[0].lon));
                    return;
                }
            } catch (err) {
                console.error('Geocoding for weather failed:', err);
            }
        }

        // Priority 3: Default to Tunis
        fetchWeatherByCoords(36.8065, 10.1815);
    }

    function toggleWeatherWidget() {
        if (!weatherContainer) return;
        if (outdoorToggle && outdoorToggle.checked) {
            weatherContainer.style.display = 'block';
            fetchWeatherForLocation();
        } else {
            weatherContainer.style.display = 'none';
        }
    }

    if (outdoorToggle) {
        outdoorToggle.addEventListener('change', toggleWeatherWidget);
        toggleWeatherWidget();
    }

    // Update weather when location or time/date changes
    if (locationInput) {
        locationInput.addEventListener('change', () => {
            if (outdoorToggle && outdoorToggle.checked) fetchWeatherForLocation();
        });
    }

    // Refresh weather when date or time changes
    document.querySelectorAll('input[name*="startAt"], select[name*="dayOfWeek"], input[name*="startTime"]').forEach(el => {
        el.addEventListener('change', () => {
            if (outdoorToggle && outdoorToggle.checked) fetchWeatherForLocation();
        });
    });


    // 7. Leaflet Map Picker Logic
    const toggleMapBtn = document.getElementById('toggle-map-picker');
    const closeMapBtn = document.getElementById('close-map-picker');
    const mapPickerContainer = document.getElementById('map-picker-container');
    const mapDiv = document.getElementById('location-map');
    
    console.log('MAP DEBUG: toggleMapBtn =', toggleMapBtn);
    console.log('MAP DEBUG: closeMapBtn =', closeMapBtn);
    console.log('MAP DEBUG: mapPickerContainer =', mapPickerContainer);
    console.log('MAP DEBUG: mapDiv =', mapDiv);

    let leafletMap, leafletMarker;

    function initLeafletMap() {
        console.log('MAP DEBUG: initLeafletMap called, leafletMap =', leafletMap);
        if (leafletMap) return;

        // Fix default marker icon (broken when Leaflet loaded via CDN)
        delete L.Icon.Default.prototype._getIconUrl;
        L.Icon.Default.mergeOptions({
            iconRetinaUrl: 'https://unpkg.com/leaflet@1.9.4/dist/images/marker-icon-2x.png',
            iconUrl: 'https://unpkg.com/leaflet@1.9.4/dist/images/marker-icon.png',
            shadowUrl: 'https://unpkg.com/leaflet@1.9.4/dist/images/marker-shadow.png',
        });

        const initialPos = [36.8065, 10.1815]; // Tunis
        
        leafletMap = L.map('location-map').setView(initialPos, 13);
        
        L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
            attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap contributors</a>'
        }).addTo(leafletMap);

        leafletMarker = L.marker(initialPos, {
            draggable: true
        }).addTo(leafletMap);

        // Click on map to move marker
        leafletMap.on('click', (e) => {
            updatePosition(e.latlng.lat, e.latlng.lng);
        });

        // Drag end handler
        leafletMarker.on('dragend', () => {
            const pos = leafletMarker.getLatLng();
            updatePosition(pos.lat, pos.lng);
        });
        
        console.log('MAP DEBUG: Leaflet map initialized successfully');
    }

    function updatePosition(lat, lng) {
        leafletMarker.setLatLng([lat, lng]);
        leafletMap.panTo([lat, lng]);

        // Store coordinates for weather
        selectedLat = lat;
        selectedLng = lng;
        
        // Use Nominatim for reverse geocoding
        fetch(`https://nominatim.openstreetmap.org/reverse?format=json&lat=${lat}&lon=${lng}`)
            .then(response => response.json())
            .then(data => {
                if (data.display_name) {
                    locationInput.value = data.display_name;
                    locationInput.dispatchEvent(new Event('change'));
                }
            })
            .catch(err => console.error('Geocoding error:', err));

        // Auto-update weather when location changes on map
        if (outdoorToggle && outdoorToggle.checked) {
            fetchWeatherByCoords(lat, lng);
        }
    }

    function loadLeafletScript() {
        console.log('MAP DEBUG: loadLeafletScript called, window.L =', !!window.L);
        if (window.L) {
            initLeafletMap();
            setTimeout(() => leafletMap.invalidateSize(), 100);
            return;
        }

        // Add Leaflet CSS
        const link = document.createElement('link');
        link.rel = 'stylesheet';
        link.href = 'https://unpkg.com/leaflet/dist/leaflet.css';
        document.head.appendChild(link);

        // Add Leaflet JS
        const script = document.createElement('script');
        script.src = 'https://unpkg.com/leaflet/dist/leaflet.js';
        script.onload = () => {
            console.log('MAP DEBUG: Leaflet script loaded, L =', !!window.L);
            initLeafletMap();
            setTimeout(() => leafletMap.invalidateSize(), 200);
        };
        script.onerror = (err) => {
            console.error('MAP DEBUG: Failed to load Leaflet script!', err);
        };
        document.head.appendChild(script);
    }

    if (toggleMapBtn) {
        console.log('MAP DEBUG: Attaching click listener to toggleMapBtn');
        toggleMapBtn.addEventListener('click', function () {
            console.log('MAP DEBUG: Carte button CLICKED!');
            if (!mapPickerContainer) {
                console.error('MAP DEBUG: mapPickerContainer is null!');
                return;
            }

            mapPickerContainer.style.display = 'block';
            console.log('MAP DEBUG: container shown, loading leaflet...');

            loadLeafletScript();

            setTimeout(() => {
                if (leafletMap) {
                    leafletMap.invalidateSize();
                    console.log('MAP DEBUG: invalidateSize called');
                }
            }, 300);
        });
    } else {
        console.error('MAP DEBUG: toggleMapBtn NOT FOUND! Button with id="toggle-map-picker" missing from DOM.');
    }

    if (closeMapBtn) {
        closeMapBtn.addEventListener('click', function () {
            if (!mapPickerContainer) return;
            mapPickerContainer.style.display = 'none';
        });
    }

    console.log('MAP DEBUG: Script fully initialized');
    updateDuration();
});
</script>
