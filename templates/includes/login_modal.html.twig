<!-- Login Required Modal -->
<div class="modal fade" id="loginRequiredModal" tabindex="-1" aria-labelledby="loginRequiredModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content" style="border-radius: 20px; border: none; box-shadow: 0 20px 40px rgba(0,0,0,0.1);">
            <div class="modal-header border-0" style="background: linear-gradient(135deg, #ec4899, #be185d); border-radius: 20px 20px 0 0;">
                <h5 class="modal-title text-white fw-bold" id="loginRequiredModalLabel">
                    <i class="fas fa-lock me-2"></i>Connexion requise
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body text-center p-4">
                <div class="my-4">
                    <i class="fas fa-heart text-pink" style="font-size: 4rem; color: #ec4899; opacity: 0.8;"></i>
                </div>
                <h4 class="fw-bold mb-3">Accédez à votre suivi personnalisé</h4>
                <p class="text-muted mb-4">
                    Pour créer ou consulter votre suivi de grossesse, vous devez être connecté à votre compte Maternia.
                </p>
                <div class="d-grid gap-3">
                    <a href="{{ path('app_login') }}" class="btn btn-primary py-3" style="background: linear-gradient(135deg, #ec4899, #be185d); border: none; border-radius: 50px;">
                        <i class="fas fa-sign-in-alt me-2"></i>Se connecter
                    </a>
                    <a href="{{ path('app_register') }}" class="btn btn-outline-secondary py-3" style="border-radius: 50px;">
                        <i class="fas fa-user-plus me-2"></i>Créer un compte
                    </a>
                </div>
                <button type="button" class="btn btn-link text-muted mt-3" data-bs-dismiss="modal">
                    Continuer sans se connecter
                </button>
            </div>
        </div>
    </div>
</div>

<style>
    .text-pink {
        color: #ec4899;
    }
    .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 20px rgba(236, 72, 153, 0.3);
    }
</style>