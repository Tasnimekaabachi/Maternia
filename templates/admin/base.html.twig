<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Backoffice{% endblock %} - Maternia</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.15.4/css/all.css">
    <link href="{{ asset('css/bootstrap.min.css') }}" rel="stylesheet">
    <link href="{{ asset('css/style.css') }}" rel="stylesheet">
    <link href="{{ asset('css/admin.css') }}" rel="stylesheet">
    {% block stylesheets %}{% endblock %}
</head>
<body class="admin-body">
    <div class="admin-wrapper">
        <aside class="admin-sidebar">
            <a href="{{ path('admin_dashboard') }}" class="admin-sidebar-brand text-decoration-none d-block">
                Mater<span>nia</span>
            </a>
            <nav class="admin-sidebar-nav">
                <a href="{{ path('admin_dashboard') }}" class="nav-link {{ app.request.attributes.get('_route') == 'admin_dashboard' ? 'active' : '' }}">
                    <i class="fas fa-tachometer-alt"></i>
                    Tableau de bord
                </a>
                <a href="{{ path('admin_suivi_grossesse') }}" class="nav-link {{ app.request.attributes.get('_route') == 'admin_suivi_grossesse' ? 'active' : '' }}">
                    <i class="fas fa-heart"></i>
                    Suivi grossesse
                </a>
                <a href="{{ path('admin_marketplace') }}" class="nav-link {{ app.request.attributes.get('_route') == 'admin_marketplace' ? 'active' : '' }}">
                    <i class="fas fa-shopping-bag"></i>
                    Marketplace
                </a>
                <a href="{{ path('admin_evenements') }}" class="nav-link {{ app.request.attributes.get('_route') == 'admin_evenements' ? 'active' : '' }}">
                    <i class="fas fa-calendar-alt"></i>
                    Événements
                </a>
                <!-- LIGNE MODIFIÉE - Réservations (Consultations) -->
                <a href="{{ path('app_admin_consultation_index') }}" 
                   class="nav-link {{ app.request.attributes.get('_route') starts with 'app_admin_consultation' ? 'active' : '' }}">
                    <i class="fas fa-calendar-check"></i>
                    Réservations
                </a>
                <a href="{{ path('admin_profil_bebe') }}" class="nav-link {{ app.request.attributes.get('_route') == 'admin_profil_bebe' ? 'active' : '' }}">
                    <i class="fas fa-baby"></i>
                    Profil bébé
                </a>
            </nav>
            <div class="p-3 border-top border-primary" style="border-color: var(--bs-border) !important;">
                <a href="{{ path('app_home') }}" class="nav-link" target="_blank">
                    <i class="fas fa-external-link-alt"></i>
                    Voir le site
                </a>
            </div>
        </aside>
        <main class="admin-main">
            <header class="admin-header">
                <h1 class="admin-header-title mb-0">{% block page_title %}Tableau de bord{% endblock %}</h1>
                <div class="admin-header-actions">
                    {% block header_actions %}{% endblock %}
                </div>
            </header>
            <div class="admin-content">
                {% block breadcrumb %}{% endblock %}
                {% block content %}{% endblock %}
            </div>
        </main>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/js/bootstrap.bundle.min.js"></script>
    {% block javascripts %}{% endblock %}
</body>
</html>