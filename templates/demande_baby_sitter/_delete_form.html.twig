<form method="post" action="{{ path('app_demande_baby_sitter_delete', {'id': demande_baby_sitter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demande_baby_sitter.id) }}">
    <button class="btn">Delete</button>
</form>
